services:
  - type: web
    name: ttrpg-finder
    env: docker
    plan: free
    buildCommand: ""
    startCommand: docker-compose up
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod